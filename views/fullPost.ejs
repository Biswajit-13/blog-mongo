<!DOCTYPE html>
<html>
<head>
  <title><%= post.title %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: "Helvetica Neue", Arial, sans-serif;
      background-color: #f9f9f9;
    }

    .container {
      padding: 30px 0;
    }

    .card {
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease;
    }

    .card-body {
      padding: 30px;
    }

    .card-title {
      font-size: 36px;
      font-weight: 600;
      color: #333;
      margin-bottom: 20px;
    }

    .card-img-top {
      width: 100%;
      height: 400px; /* Set a fixed height for the image */
      object-fit: cover; /* Maintain aspect ratio and cover container */
      /* border-radius: 10px; */
      margin-bottom: 20px;
    }

    .card-text {
      color: #666;
      line-height: 1.8;
      font-size: 18px;
    }

    .muted {
      color: #999;
    }

    .font-5 {
      font-size: 14px;
    }

    .hashtag {
      color: #ff028d;
    }

    .btn-back {
      background-color: #428bca;
        border-color: #428bca;
        border-radius: 20px;
        padding: 8px 20px;
        font-weight: bold;
        color: white;
    }

    .btn-back:hover {
      background-color: #3071a9;
        border-color: #3071a9;
    }
  </style>
</head>
<body>
  <div class="container py-4">
    <div class="card">
      <% if (post.blogPicture) { %>
      <img
        src="<%= post.blogPicture %>"
        class="card-img-top"
        alt="Blog Picture"
      />
      <% } %>
      <div class="card-body">
        <h2 class="card-title"><%= post.title %></h2>
        <p class="card-text"><%= post.content %></p>
        <p class="muted font-5">Posted on <%= post.date.toDateString() %></p>
        <p class="muted font-5"><span class="hashtag">#<%= post.category %></span></p>
        <a href="/posts" class="btn btn-back">Back to All Posts</a>
      </div>
    </div>
  </div>

  <!-- Bootstrap JavaScript (place it before the closing </body> tag) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
